class ScheduleActivity {
  final DateTime date;
  final DateTime hour;
  final int totalParticipants;

  ScheduleActivity(
      {required this.date,
      required this.hour,
      required this.totalParticipants});
}
